def palindrome(string):
    if string == string [::-1]:
        print "yes it is palindrome"
    else:
        print "no the word you entered is not a palindrome"
palindrome(str(raw_input("String ")).upper())

